{
  "name": "ezone-staffing",
  "version": "1.0.0",
  "private": true,
  "description": "E-ZONE staffing manager — cloud version (Express proxy + Apps Script + Google Sheets)",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
