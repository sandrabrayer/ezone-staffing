# Payroll-verified fixes — the runbook

Everything in this document acts on facts checked against E-ZONE's
accounting payroll export (Jan–Aug 2026). Nothing here is inferred by the
app, and nothing here is guessed: a fact that cannot be resolved to exactly
one worker stops the whole run.

All of it lives in `apps-script/Code.gs`, is run from the **Apps Script
editor** (never over HTTP — no `doPost` action reaches any of it, and a
test pins that), and every function that can write **is a dry run unless you
pass the literal `false`**.

---

## The order to run things

| # | Run | What it does | Writes? |
|---|---|---|---|
| 1 | `logMonthTotalsNow()` | the **baseline** for 08 / 09 / 10 2026 | no, ever |
| 2 | `applyVerifiedFixesNow()` | prints the plan for A, C, D, E | no |
| 3 | `applyVerifiedFixesNow(false)` | applies it | yes |
| 4 | `logMonthTotalsNow()` | what moved, and by how much | no |
| 5 | `writeMissingAssignmentsTabNow()` | builds «שיבוצים חסרים» | the proposal tab only |
| 6 | *Moran fills the sheet in* | house, role, type, rate, date, «אשר» | — |
| 7 | `applyMissingAssignmentsNow()` | prints the plan | no |
| 8 | `applyMissingAssignmentsNow(false)` | creates the approved placements | yes |
| 9 | `logMonthTotalsNow()` | the new totals | no |

Steps 1, 4 and 9 are the point of the exercise: **every shekel that moves is
attributable to one named run.** Take a screenshot of each log, or copy it
somewhere — the logs are the audit trail of the operation itself, on top of
the per-field `audit_log` rows each write leaves behind.

---

## `logMonthTotalsNow(months)` — read-only

```js
logMonthTotalsNow()                      // 2026-08, 2026-09, 2026-10
logMonthTotalsNow(['2026-01','2026-02'])
```

Logs, per month: the total, the three buckets, whether any real hours or
sessions were recorded, and a per-house breakdown with budget and variance.
It **writes nothing at all** — not a tab, not the audit log — so it is always
safe, and a test asserts that it touches nothing.

The numbers come from `lib/cost-engine.js` itself. `apps-script/CostEngine.gs`
is a generated byte-for-byte copy (`node scripts/sync_cost_engine_gs.js`) that
clasp deploys beside `Code.gs`, and `tests/cost-engine-gs-sync.test.js` fails
the build if the two ever drift. A baseline taken with a second, similar
implementation would prove nothing about what Moran sees on screen.

---

## `applyVerifiedFixesNow(dryRun)` — A, C, D, E

The verified facts are **data** at the top of the section in `Code.gs`
(`VERIFIED_START_DATE_FIXES`, `VERIFIED_LEAVERS`, `VERIFIED_START_DATE_FLOORS`,
`VERIFIED_RENAMES`), so what was checked is readable without reading any
logic.

### A · the future-start typo

`wmppe95x5vd8o` שובל לובטון — payroll employee #56, paid every month since
January 2026. Start date corrected to **2026-01-01**, as a confirmed date
(no source tag). **His assignments are not touched.** The run refuses to act
if the id and the name disagree, so a copied id can never land a date on
someone else's row.

### C · the four leavers

Last seen in the January payroll: שלומציון כהן · דן רובינסון · ניר לוי ·
שלי הלפט. Each worker row is **moved** to `workers_archive` with the reason
«לא מופיע/ה בדוח השכר מאז ינואר 2026» — archived, **never deleted**. A leaver
who still holds a placement **aborts the run**: the payroll says gone, the
sheet says placed, and that contradiction is for a person to resolve.

### D · eight payroll-floor start dates

The first month each worker appears in the payroll book. The true start is
**that month or earlier**, so what is written is the 1st of that month *and*
`start_date_source = 'payroll_floor'`:

| | |
|---|---|
| 2026-01 | sergei makarov · שחר מזור · מעיין דלומי · חנן וייל |
| 2026-02 | אלה שפירא |
| 2026-03 | עדי איזנברג |
| 2026-05 | בן ציון אדרי |
| 2026-06 | אורן סילמניק |

**A floor is not a date.** It never overwrites a date a person entered, it
shows a grey dashed «תאריך משוער» chip wherever it appears, it carries a
«מקור התאריך» column into the roster and unassigned exports, and the
integrity report keeps the worker visible with an `ESTIMATED_START_DATE`
(info) row. Typing the exact date later **clears the tag automatically** —
a person's answer outranks a reconstruction, and that clearing is audited
too. See rule 9 in [COST_RULES.md](COST_RULES.md) for what it does to the
money (short version: nothing — only to what the page may claim).

### E · the rename

«אתי (אסתר) דבוש» → «אתי אסתר דבוש». Parentheses in a Hebrew name break
copy-paste rendering in a bidi context, and every consumer app matches this
person by exact name.

### F · the ~20 freelancers — deliberately untouched

Per-session therapists and freelancers are invoice-based and do not appear
in the payroll book at all. **No date is invented for them.** They keep their
`MISSING_START_DATE` finding and their amber chip until somebody knows.

### Fail loudly

A verified fact names a person. If the name resolves to **no** worker, or to
**more than one**, the whole run aborts **before a single write** — in a dry
run exactly as in an apply run, so an ambiguity is found while it is still
cheap. The log says which name and which ids, and nothing else in the batch
is applied either: a partially applied batch would be the worst of both.

Running it twice is free: each fact recognises its own result and reports
"already in place" rather than failing to find a name it has itself changed.

---

## «שיבוצים חסרים» — B, the ten who are paid and unplaced

רון מנחם · דניאל קוטסי · בר ליידרמן · עידו בוזגלו · שירן כהן · אופק רחמים ·
אופיר רוטנברג · ניב מנחם סין · דפנה כץ · דניאל סייג

They are paid in the August payroll and hold no assignment at all, so they
contribute **0** to every month's cost. That gap **cannot** be closed
automatically: an assignment needs a house, an employment type and a rate,
and the app holds none of the three for these people. Inventing any of them
would replace a visible hole with an invisible wrong number.

`writeMissingAssignmentsTabNow()` builds one row per worker, pre-filled with
the id and the name and **nothing else**:

| Column | Filled by |
|---|---|
| `מזהה עובד`, `שם העובד/ת` | the app |
| `מחלקה בשכר` | Moran, from a dropdown of the six payroll departments |
| `בית מוצע (הצעה בלבד)` | computed from the department — **a suggestion, never written anywhere** |
| `בית`, `תפקיד`, `סוג העסקה` | Moran, from dropdowns |
| `סכום`, `כמות` | Moran — what they mean per type is in the «מה חסר» column |
| `תאריך תחילת השיבוץ` | Moran — becomes the placement's `effective_from` |
| `אשר` | Moran — `כן` / `לא` |
| `מה חסר` | computed on every rebuild: exactly what the row still needs |

Dropdown values carry the ASCII id **and** the Hebrew (`ramot · רמות השבים`),
so nothing is ever matched back by guessing at a label.

Work already entered **survives a rebuild**, keyed by worker id. A worker who
gains an assignment **drops off the sheet by itself**, so it is always the
open work and never a stale checklist.

### The department mapping — reference only

| Code | Payroll department | House |
|---|---|---|
| 001 | רעננה פרדס | `pardes` |
| 002 | קיסריה | **none** — covers both `ofroni` and `rehab`, a person must choose |
| 003 | רמות השבים | `ramot` |
| 004 | מטה | `hq` |
| 005 | רעננה אשר | `asher` |
| 006 | הולינה | **none** — no house in the app at all |

It fills the suggestion column and **nothing else**. A row with a department
and no house creates nothing, however approved it is.

### `applyMissingAssignmentsNow(dryRun)`

Creates an assignment **only** from a row that is `אשר = כן` **and**
completely filled in, through `addAssignment` — the same path the app uses,
so the same validation, the same duplicate `(worker, house)` refusal and the
same `gmach_month` sync apply. Each creation lands in the audit log with the
reason «תיקון מאומת מול דוח שכר».

A partial row is **skipped and named**, with exactly what it still needs, in
the same words the sheet uses. Nothing is defaulted, ever.

---

## What none of this does

- It does not touch anyone's assignments except to **create** the ones
  approved on the proposal sheet.
- It does not delete a worker. Archiving **moves** the row to
  `workers_archive`, which is append-only.
- It does not invent a start date for anyone outside list D.
- It does not write a house from a payroll department.
- It does not run over HTTP. There is no action for any of it.
